# AetherBrowser

AI-first browser runtime and extension extracted from SCBE-AETHERMOORE.

## What this repo contains
- Python runtime server (`src/aetherbrowser`)
- Browser extension UI (`src/extension`)
- Focused tests for runtime + extension edge cases (`tests/`)

## Current status
- Streaming model bridge + provider fallback
- Research routing lane with persistence hooks
- WebSocket reconnect indicator + queue pressure handling
- Targeted tests passing in source monorepo lane

## Monetization direction
- Open-core browser runtime (public)
- Pro lanes: managed cloud orchestration, team controls, hosted research vault
- Enterprise lane: compliance logging + governance policy packs

## Immediate roadmap
1. Stabilize standalone CI in this repo.
2. Publish prebuilt extension package.
3. Add hosted API + paid plans.
4. Add docs and demo videos for conversion.

## Source provenance
This repository is seeded from `issdandavis/SCBE-AETHERMOORE`.
